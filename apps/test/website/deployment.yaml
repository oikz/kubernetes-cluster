apiVersion: apps/v1
kind: Deployment
metadata:
  name: website
spec:
  template:
    spec:
      containers:
        - name: website
          env:
            - name: OTEL_SERVICE_NAME
              value: 'Website - Test'
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: test-database-app
                  key: host
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: test-database-app
                  key: password