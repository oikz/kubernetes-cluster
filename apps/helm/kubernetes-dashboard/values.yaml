app:
  ingress:
    enabled: true
    hosts: ["kubernetes.oikz.dev"]
    useDefaultIngressClass: true
    useDefaultAnnotations: false
    annotations:
      traefik.ingress.kubernetes.io/router.entrypoints: websecure
    tls:
      enabled: false
kong:
  proxy:
    http:
      enabled: true